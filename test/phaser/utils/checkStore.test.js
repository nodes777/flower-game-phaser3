import {
	checkStore,
	getStoreState
} from "../../../src/phaser/utils/checkStore";

describe.skip("checkStore", () => {
	it("Should see if there's any changes in the store", () => {});
});
describe.skip("getStoreState", () => {
	it("Should return the state", () => {});
});
