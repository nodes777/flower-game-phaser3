const {
	addBeeFrameRotation
} = require("../../../src/phaser/utils/addBeeFrameRotation");

describe.skip("addBeeFrameRotation", () => {
	it("Should rotate the 3D bee frame, based on the velocity", () => {});
});
