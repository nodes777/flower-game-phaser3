export function update() {
	// this.bee.rotation = Phaser.Math.Angle.Between(
	// 	this.bee.x,
	// 	this.bee.y,
	// 	this.firstFlower.x,
	// 	this.firstFlower.y
	// );
	// check for new flowers
	// if new flowers, add them to the scene
	// 		add new flowers for bee fly to
	// if bee collided with current target
	// 		set target to new flower
}
